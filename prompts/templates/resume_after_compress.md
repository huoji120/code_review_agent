请从压缩后的上下文恢复审计。恢复后优先更新详细中文 todo 和文件排查状态：调用 todo_update/todo_create 或 file_review_update，标题/备注必须包含具体文件/模块/入口/变量/审计点。已经完成的 todo 必须调用 todo_update 设置 status:"completed"，未完成或证据未闭合的 todo 不要提前标记完成。若存在未闭合的跨文件链路，调用 flow_review_update 更新入口、文件链、变量、证据和下一步。然后按照 `后续 !{summary_interval} 轮计划` 继续推进。工具调用必须且只能使用 <tool_call>{...}</tool_call> JSON，禁止 <invoke>/<parameter>。所有说明使用中文。如果压缩摘要标记某个方向“连续20轮无证据”，恢复后禁止继续重复搜索、重复读同一批文件或重复追同一条无进展链路，必须换一个新的入口、模块、变量、权限点或利用链方向。
