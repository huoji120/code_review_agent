# 进入执行审计阶段

规划阶段已经完成，下面是规划阶段通过 `audit_plan_done` 提交的审计地图、审计范围和执行指令。规划阶段对话已经隔离丢弃；你现在必须只按执行阶段系统提示词和下面的规划产物工作。

## 规划结果

!{plan_result}

## 当前排查状态

!{review_state}

执行要求：

- 必须优先按规划结果里的 audit_files、audit_map 和 todo 顺序审计。
- 不要重新做规划，不要扩大到全仓库盲扫；只有当当前 todo 证据链需要补充时，才读取相邻文件或搜索具体符号。
- 每条候选链路必须补齐入口、权限/边界、变量传播、关键校验、sink 和实际影响。
- 如果某个规划候选不成立，必须用 todo_update 或 flow_review_update 写清楚断点，然后切到下一个规划优先级。
- 只有证据闭合、危害明确、置信度高时才调用 verify_finding 和 report_finding。
