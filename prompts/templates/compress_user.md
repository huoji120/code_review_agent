!{state_and_conversation}

请在压缩摘要中增加 `后续 !{summary_interval} 轮计划` 字段，逐条规划接下来 !{summary_interval} 轮应该做什么，并说明需要更新哪些 todo 和文件排查状态。压缩时只输出文字总结，禁止调用任何工具。如果发现某个方向连续20轮都没有找到任何新证据、没有新命中、没有推进利用链或只是重复搜索同一对象，必须在摘要中明确写出“连续20轮无证据”，并要求恢复后停止继续沿该方向空转，改换审计目标。
