请从压缩后的上下文恢复【规划建图阶段】。恢复后继续绘制审计地图、创建具体 todo、标记本次 one-shot 要审计的文件范围，并优先检查当前项目笔记是否需要补充。项目笔记是自由文本工作笔记，越详细越好；读到新的架构、行为、登录认证、鉴权、入口、攻击面、数据/状态流或关键文件角色信息后，必须积极调用 project_note_update 更新。禁止提交漏洞、禁止结束审计、禁止进入漏洞验证。规划完成后只能调用 audit_plan_done 切换到执行阶段。工具调用必须且只能使用 <tool_call>{...}</tool_call> JSON，禁止 <invoke>/<parameter>。所有说明使用中文。如果压缩摘要标记某个方向“连续20轮无证据”，恢复后禁止继续重复同一方向，必须换一个文件地图维度、入口类型、信任边界或审计优先级。
